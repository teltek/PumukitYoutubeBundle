<select id="youtube_playlist_label" name="youtube_playlist_label" class="form-control" required>
    {% for item in playlist %}
        <option value="{{ item.getId() }}">{{ item.getName() }}</option>
    {% endfor %}
</select>